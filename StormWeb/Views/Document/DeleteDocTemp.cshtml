@model StormWeb.Models.Template_Document

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Template_Document</legend>

    <div class="display-label">Course</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Course.Course_Name)
    </div>

    <div class="display-label">Form_Name</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Form_Name)
    </div>

    <div class="display-label">Path</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Path)
    </div>

    <div class="display-label">UploadedBy</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.UploadedBy)
    </div>

    <div class="display-label">UploadedOn</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.UploadedOn)
    </div>

    <div class="display-label">FileName</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.FileName)
    </div>

    <div class="display-label">Comment</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Comment)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "ShowAllDocumentTemp", new { id = ViewBag.ReturnCourseId })
    </p>
}
