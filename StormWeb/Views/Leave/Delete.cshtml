@model StormWeb.Models.LeaveApplication
@{
    ViewBag.Title = "Delete";
}
<h3>
    Are you sure you want to delete this?</h3>
<br />
@Html.ValidationSummary(false, "", new { @class = "error" })
<div class="row-fluid">
    <div class="span12">
        <div class="box-tab">
            <div class="page-header">
                <h1>
                    &nbsp;&nbsp;Leave Details
                </h1>
            </div>
            <div class="widget-content">
                <div class="widget-box">
                    <br />
                    <table class="table">
                        <tr>
                            <th>StartDate
                            </th>
                            <td>  @Html.DisplayFor(model => model.StartDate)
                            </td>
                        </tr>
                         <tr>
                            <th>StartDate
                            </th>
                            <td>@Html.DisplayFor(model => model.StartDate)
                            </td>
                        </tr>
                         <tr>
                            <th>EndDate
                            </th>
                            <td> @Html.DisplayFor(model => model.EndDate)
                            </td>
                        </tr>
                         <tr>
                            <th>Status
                            </th>
                            <td>@Html.DisplayFor(model => model.Status)
                            </td>
                        </tr>
                         <tr>
                            <th>Comment
                            </th>
                            <td>        @Html.DisplayFor(model => model.Comment)
                            </td>
                        </tr>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
 <p>
    <input type="submit" value="Delete" class="btn btn-danger  tip-top" data-toggle="modal"
        href="#myModal1" data-original-title="Delete Leave Details" />

    @Html.Button_BackToList("Bact To List", "../../Leave")
</p>
 
@using (Html.BeginForm("Delete", "Leave", FormMethod.Post))
{   
    <div class="modal hide fade" id="myModal1" style="width: 250px">
        <div class="modal-header">
            <button type="button" class="close" data-dismiss="modal">
                ×</button>
            <h3>
                Delete Leave Details</h3>
        </div>
        <div class="modal-body">
            <p>
                Are you sure you want to delete this ?
            </p>
        </div>
        <div class="modal-footer">
            <a href="#" class="btn" data-dismiss="modal" id="dismissModal">No</a>
            <input type="submit" class="btn btn-modal" id="save" type="submit" value="Yes" />
        </div>
    </div>
}
