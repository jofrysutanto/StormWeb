@model StormWeb.Models.Client_Children_Detail
@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_LayoutEmpty.cshtml";
}
<h2>
   Add Childrens Details</h2>
<br />
<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>
@using (Html.BeginForm("AddChildrensDetails", "StudentCentre", FormMethod.Post))
{
    @Html.ValidationSummary(false, null, new { @class = "error" })
  
    <div id="divEditProfile">
        <table style="text-align: left;"> 
            <tr>
                <th>
                    Given Name
                </th>
                <td>
                    @Html.TextBoxFor(model => model.Given_Name)
                    @Html.ValidationMessageFor(model => model.Given_Name)
                </td>
            </tr>
            <tr>
                <th>
                    Surname
                </th>
                <td>
                    @Html.TextBoxFor(model => model.Sur_Name)
                    @Html.ValidationMessageFor(model => model.Sur_Name)
                </td>
            </tr>
            <tr>
                <th>
                    DOB
                </th>
                <td>
                    @Html.TextBoxFor(model => model.DOB, new { @class = "chooseDate", @readonly = "readonly" })
                </td>
            </tr>
            <tr>
                <th>
                    Gender
                </th>
                <td>
                    <div class="control-group">
                        <div class="controls">
                            @Html.DropDownListFor(model => model.Gender, (SelectList)ViewData["Gender"], new { @class = "chzn-select" })
                            <p class="help-block">
                                @Html.ValidationMessageFor(model => model.Gender)
                            </p>
                        </div>
                    </div>
                </td>
            </tr>
            <tr>
                <th>
                    Notes
                </th>
                <td>
                    @Html.TextBoxFor(model => model.Notes)
                </td>
            </tr>
        </table>
        <div class="modal-footer">
            <input type="submit" class="btn btn-primary" value="Save Changes" />
            <input type="button" id="closeModal" class="btn btn-danger cancelButton" value="Cancel" />
        </div>
    </div>
} 
<script type="text/javascript">
    $(function () {
        $(".cancelButton").click(function () {
            parent.$.fn.colorbox.close();
        });
    });  

    
    $(function(){
    @{
        if (ViewBag.successEdit != null)
        {
     //   @:parent.location='/StudentCentre/Profile/@(Model.caseTable.First().Student_Id)?message=edit';
        @:parent.$.fn.colorbox.close();
        };
    }
     });
      
       $(document).ready(function () { 
        $(".chooseDate").datepicker({
            dateFormat: 'mm/dd/yy',
            changeMonth: true,
            changeYear: true,
            minDate: '-100Y',
            maxDate: '0'
        });
    });
     
</script>
